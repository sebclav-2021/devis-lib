  <div class="title">
    <h1>Coordonnées du client</h1>
  </div>

<div>
  <div class="form">
    <%= simple_form_for(@customer) do |f| %>
      <div class="form-inputs form-control-sm">
        <%= f.input :name, label: "Prénom et Nom :" %>
        <%= f.input :address, label: "Adresse :" %>
        <%= f.input :email, label: "E-mail :" %>
        <%= f.input :phone, label: "Téléphone :" %>
      </div>

      <div class="form-actions">
        <div class="form">

        </div>
      </div>
    <% end %>
  </div>
</div>

 <%= simple_form_for(@quote) do |f| %>
              <%= f.hidden_field :customer, value: @customer.id %>
            <div class="form-actions">
              <%= f.submit 'Continuer'%>
            </div>
  <% end %>
